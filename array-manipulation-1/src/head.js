/* exported head */

function head(array) {

  var first = array[0];

  return first;
}

// define a function, "head", that passes one parameter, "array"

// assign the first index of "array" to a variable, "first"

// return first
