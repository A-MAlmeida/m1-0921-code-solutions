/* exported last */

function last(array) {

  var lastIndex = array[array.length - 1];

  return lastIndex;

}

// define a function, "last", that passes one parameter, "array"

// take the value of "array" at the length of "array" -1 and assign that to a variable, "lastIndex"

// return lastIndex
