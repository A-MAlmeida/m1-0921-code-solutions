/* exported getFirstChar */

function getFirstChar(string) {

  var firstChar = string.charAt(0);

  return firstChar;
}

// define a function, "getFirstChar", that receives one parameter, "string"

// use a method on the "string" object that selects the first character of the string and assign the result of that expression to a variable named "firstChar"

// return firstChar
