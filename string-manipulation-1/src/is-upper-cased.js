/* exported isUpperCased */

function isUpperCased(word) {

  var upper = false;

  if (word === word.toUpperCase()) {

    return true;
  }

  return upper;
}

// define a function, "isUpperCased", that received one parameter, "word"

// define a variable with a value of boolean 'false'

// make an "if" statement with an argument that checks if the parameter "word" is strictly equal to the whole parameter "word" being capitalized

// return true within the "if" code block

// return upper outside of the "if" code block
