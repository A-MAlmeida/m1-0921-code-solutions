/* exported getLastChar */

function getLastChar(string) {

  var lastChar = string.charAt(string.length - 1);

  return lastChar;
}

// define a function, "getLastChar", that receives one parameter, "string"

// use a method on the "string" object that passes the length of the "string" object -1 as an argument and assign the result of that expression to a variable named lastChar

// return lastChar
